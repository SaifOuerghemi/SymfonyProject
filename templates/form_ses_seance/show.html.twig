{% extends 'base.html.twig' %}

{% block title %}FormSesSeance{% endblock %}

{% block body %}
    <h1>FormSesSeance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form_ses_seance.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('form_ses_seance_index') }}">back to list</a>

    <a href="{{ path('form_ses_seance_edit', {'id': form_ses_seance.id}) }}">edit</a>

    {{ include('form_ses_seance/_delete_form.html.twig') }}
{% endblock %}
